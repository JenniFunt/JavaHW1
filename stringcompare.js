// Create a conditional statement that checks if two strings are equal.
//Include an if and else statement. Create an alert() that gives feedback based
//on the string defined.
// Test your statement with different strings to make sure both alerts work.

var phrase = "Jenni Loves Pizza";

if (phrase === "Jenni Loves Pizza"){
    console.log("you're absolutely right!");
  }else{
    alert("Wrong, try again");
    console.log("Nope");
  }
